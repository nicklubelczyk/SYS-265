---
- name: Disable Unnecessary Services (Windows Linux) and Minimize Firewall
  hosts: all
  become: yes
  tasks:
    # Disable CUPS and Spooler Services
    - name: Stop CUPS service Linux
      ansible.builtin.systemd:
        name: cups
        state: stopped
        enabled: no
      when: ansible_os_family == "RedHat" or ansible_os_family == "Debian"

    - name: Disable Spooler service Windows
      ansible.windows.win_service:
        name: Spooler
        state: stopped
        start_mode: disabled
      when: ansible_os_family == "Windows"

    # Enable Only Minimal Firewall Rules
    - name: Windows Firewall
      ansible.posix.firewalld:
        port: "{{ item }}/tcp"
        permanent: yes
        state: enabled
      loop:
        - "22"
        - "3389"
        - "10000"
      when: ansible_os_family == "RedHat"

    - name: Windows Firewall
      ansible.windows.win_shell: |
        New-NetFirewallRule -DisplayName "Allow SSH" -Direction Inbound -LocalPort 22 -Protocol TCP -Action Allow
        New-NetFirewallRule -DisplayName "Allow RDP" -Direction Inbound -LocalPort 3389 -Protocol TCP -Action Allow
      when: ansible_os_family == "Windows"
